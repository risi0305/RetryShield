# Loaded automatically by `vite build` (production mode only) — local `vite dev`
# is unaffected and keeps using the dev proxy to localhost:4000 in vite.config.ts.
VITE_API_URL=https://retryshield-1.onrender.com/api
